package test;
import java.util.*;


public class CacheManager {
    private int maxsize;
    private boolean isLRU
    private HashSet<String> cachedwords ;
    private CacheReplacementPolicy ;

    public CacheManager(int maxsize, CacheReplacementPolicy crptype) {
        this.maxsize = maxsize;
        this.cachedwords = new  HashSet<String>();
        this.crp = crptype
        if(crptype.getCalass().equals("LRU"))

    }




    public void add(string word) {
        if (cachetype.)
        CacheManager.add(key, value);
    }


}

